---
title: Card
description: Card
lastUpdated: 2024-04-15
---

import CWFCode from "../../../components/CWFCode.astro";

export const example1 = `<div class="card">
  <div class="card__header">Card header</div>
  <div class="card__body">This is a simple card.</div>
  <div class="card__footer">Card footer</div>
</div>`;
export const example1Style = `
.card {
  background-color: var(--card-background-color, white);
  border-color: var(--card-border-color, LightGrey);
  border-radius: var(--card-border-radius, 4px);
  border-style: var(--card-border-style, solid);
  border-width: var(--card-border-width, 1px);
  display: var(--card-display, flex);
  flex-flow: var(--card-flex-flow, column nowrap);
  margin: var(--card-margin-top, 0) var(--card-margin-right, auto)
    var(--card-margin-bottom, 1ch) var(--card-margin-left, auto);
  min-width: var(--card-min-width, 0);
  position: var(--card-position, relative);
  word-wrap: var(--card-word-wrap, break-word);

  & > * {
    &:first-child {
      border-top-right-radius: var(--card-inner-border-radius, 3px);
      border-top-left-radius: var(--card-inner-border-radius, 3px);
    }

    &:last-child {
      border-bottom-right-radius: var(--card-inner-border-radius, 3px);
      border-bottom-left-radius: var(--card-inner-border-radius, 3px);
    }
  }

  & .card__header,
  & .card__body,
  & .card__footer {
    padding: var(--card-inner-padding-y, 1ch) var(--card-inner-padding-x, 2ch);
  }

  & .card__header {
    background-color: var(--card__header-background-color, white);
    border-bottom-color: var(--card__header-border-bottom-color, LightGrey);
    border-bottom-style: var(--card__header-border-bottom-style, solid);
    border-bottom-width: var(--card__header-border-bottom-width, 1px);
  }

  & .card__body {
    flex: var(--card__body-flex, 1 1 auto);
  }

  & .card__footer {
    background-color: var(--card__footer-background-color, white);
    border-top-color: var(--card__footer-border-top-color, LightGrey);
    border-top-style: var(--card__footer-border-top-style, solid);
    border-top-width: var(--card__footer-border-top-width, 1px);
    margin-top: var(--card__footer-margin-top, auto);
  }
}
`;

<CWFCode code={example1} style={example1Style} />
